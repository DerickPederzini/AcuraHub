import 'package:flutter/material.dart';

class AppIcons {
  static const likeIcon = Icon(Icons.heart_broken);
  static const commentIcon = Icon(Icons.comment);
  static const shareIcon = Icon(Icons.share);
  static const starIcon = Icon(Icons.star);
}